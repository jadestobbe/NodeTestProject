environment:
    matrix:
        - nodejs_version: "9"

install:
    - ps: Install-Product node $env:nodejs_version
    - npm config set unsafe-perm true
    - npm install

test_script:
    - node --version
    - node --version
    - npm test

build: off
